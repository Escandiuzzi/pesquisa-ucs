---
import type { HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"a"> {}

const { class: className, ...props } = Astro.props;
---

<a class:list={["hover:underline active:opacity-70", className]} {...props}
  ><slot /></a
>
